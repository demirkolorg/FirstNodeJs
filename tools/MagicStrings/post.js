const getAll = {
    getAllGenelHata: "Kayıtları listeleme işlemi sırasında bir hata meydana geldi",
}

const getDetail = {
    getDetailGenelHata: "Kayıt listeleme işlemi sırasında bir hata meydana geldi",
}

const Create = {
    createGenelHata: "Ekleme işlemi sırasında bir hata meydana geldi",
}

const Update = {
    updateGenelHata: "Güncelleme işlemi sırasında bir hata meydana geldi",
}

const Delete = {
    deleteGenelHata: "Silme işlemi sırasında bir hata meydana geldi",
}


module.exports = { getAll, getDetail,Create,Update,Delete }
